# .env file
# URL Shortener 环境配置文件

# 创建 .env 文件并根据需要修改以下配置

## 基础配置
ENV_NAME="Local"
BASE_URL="http://127.0.0.1:8000"
DB_URL="sqlite:///./shortener.db"

## JWT 配置 (可选，不填则每次启动自动生成随机密钥)
# JWT_SECRET_KEY="your-secret-key-here-keep-it-safe"
# JWT_ALGORITHM="HS256"
# ACCESS_TOKEN_EXPIRE_MINUTES=1440

## 说明
# 1. ENV_NAME: 环境名称，用于区分开发/测试/生产环境
# 2. BASE_URL: 服务基础地址，用于生成短链地址
# 3. DB_URL: 数据库连接地址，支持 SQLite、PostgreSQL 等
# 4. JWT_SECRET_KEY: JWT 签名密钥，生产环境请务必设置固定值
# 5. ACCESS_TOKEN_EXPIRE_MINUTES: Token 过期时间，默认 1440 分钟 (24 小时)

## 首次使用步骤
# 1. 安装依赖: pip install -r requirements.txt
# 2. 创建管理员账号: python init_admin.py
# 3. 启动服务: uvicorn shortener_app.main:app --reload
# 4. 访问: http://localhost:8000

# .env file
# URL Shortener 环境配置文件

# 创建 .env 文件并根据需要修改以下配置

## 基础配置
ENV_NAME="Production"
BASE_URL="http://127.0.0.1:8000"
DB_URL="sqlite:///./shortener.db"

## JWT 配置 (生产环境必须设置固定值)
JWT_SECRET_KEY="your-32-character-random-secret-key-change-it"
JWT_ALGORITHM="HS256"
ACCESS_TOKEN_EXPIRE_MINUTES=1440

## 注册控制 (默认关闭公开注册)
ALLOW_PUBLIC_REGISTRATION=false
# 可选：设置邀请码，只有知道邀请码的人才能注册
# INVITE_CODE="your-team-secret-code"

## 密码强度配置
MIN_PASSWORD_LENGTH=8

## 登录安全配置
MAX_LOGIN_ATTEMPTS=5
ACCOUNT_LOCKOUT_MINUTES=15
FAILED_LOGIN_RESET_MINUTES=30

## 说明
# 1. ENV_NAME: 环境名称，用于区分开发/测试/生产环境
# 2. BASE_URL: 服务基础地址，用于生成短链地址
# 3. DB_URL: 数据库连接地址，支持 SQLite、PostgreSQL 等
# 4. JWT_SECRET_KEY: JWT 签名密钥，生产环境请务必设置一个长且随机的字符串
# 5. ACCESS_TOKEN_EXPIRE_MINUTES: Token 过期时间，默认 1440 分钟 (24 小时)
# 6. ALLOW_PUBLIC_REGISTRATION: 是否允许公开注册，默认 false
# 7. INVITE_CODE: 邀请码（可选），设置后需要邀请码才能注册
# 8. MIN_PASSWORD_LENGTH: 最小密码长度，默认 6
# 9. MAX_LOGIN_ATTEMPTS: 最大登录失败次数，默认 5 次，超过后锁定账号
# 10. ACCOUNT_LOCKOUT_MINUTES: 账号锁定时间，默认 15 分钟
# 11. FAILED_LOGIN_RESET_MINUTES: 失败次数自动重置时间（暂时未使用）

## 首次使用步骤
# 1. 安装依赖: pip install -r requirements.txt
# 2. 创建 .env 文件并配置 (可选)
# 3. 创建管理员账号: python init_admin.py
# 4. 启动服务: uvicorn shortener_app.main:app --reload
# 5. 访问: http://localhost:8000

## 注册模式说明
# 
# 【模式 1：完全关闭注册】（推荐，最安全）
# ALLOW_PUBLIC_REGISTRATION=false  (默认)
# 不设置 INVITE_CODE
# 
# 效果：注册功能完全关闭，只有管理员能创建用户
# 使用：管理员登录后通过 API 或 /docs 页面创建新用户
#

# 【模式 2：邀请码模式】
# ALLOW_PUBLIC_REGISTRATION=true (可选，设不设都可以)
# INVITE_CODE="your-secret-invite-code"
#
# 效果：用户可以注册，但必须输入正确的邀请码
# 使用：把邀请码告诉团队成员，他们注册时输入即可
#

# 【模式 3：公开注册】（不推荐用于内部服务）
# ALLOW_PUBLIC_REGISTRATION=true
# 不设置 INVITE_CODE
#
# 效果：任何人都可以注册，无限制

## 登录安全机制
# 
# 1. 连续失败锁定：
#    - 连续 MAX_LOGIN_ATTEMPTS (默认 5) 次密码错误
#    - 账号将被锁定 ACCOUNT_LOCKOUT_MINUTES (默认 15) 分钟
#    - 锁定期间即使输入正确密码也无法登录
#    - 返回 423 状态码，提示剩余锁定时间
#
# 2. 失败次数提示：
#    - 每次密码错误会提示剩余尝试次数
#    - 例如："Incorrect username or password. 3 attempts remaining."
#
# 3. 登录成功重置：
#    - 一旦登录成功，失败次数会被重置为 0
#
# 4. 管理员解锁：
#    - 管理员可以通过 PUT /admin/users/{user_id}/unlock 接口提前解锁
#
# 5. Token 撤销：
#    - 登出时调用 POST /auth/logout 会将当前 Token 加入黑名单
#    - 被撤销的 Token 无法再访问受保护的 API
#    - 每个 Token 有唯一的 jti (JWT ID) 用于追踪
#

## 管理员功能
# 管理员登录后可以:
# 1. 通过 POST /admin/users 创建新用户
# 2. 通过 GET /admin/users 查看所有用户列表
# 3. 通过 PUT /admin/users/{user_id}/toggle-active 启用/禁用用户
# 4. 通过 PUT /admin/users/{user_id}/unlock 提前解锁被锁定的用户
# 5. 通过 PUT /admin/users/{user_id}/reset-password 重置用户密码
# 6. 管理所有用户的短链

# .env file
# URL Shortener 环境配置文件

# 创建 .env 文件并根据需要修改以下配置

## 基础配置
ENV_NAME="Production"
BASE_URL="http://127.0.0.1:8000"
DB_URL="sqlite:///./shortener.db"

## JWT 配置 (生产环境必须设置固定值)
JWT_SECRET_KEY="your-strong-secret-key-here-change-it"
JWT_ALGORITHM="HS256"
ACCESS_TOKEN_EXPIRE_MINUTES=1440

## 注册控制 (默认关闭公开注册)
ALLOW_PUBLIC_REGISTRATION=false
# 可选：设置邀请码，只有知道邀请码的人才能注册
# INVITE_CODE="your-team-secret-code"

## 密码强度配置
MIN_PASSWORD_LENGTH=8

## 说明
# 1. ENV_NAME: 环境名称，用于区分开发/测试/生产环境
# 2. BASE_URL: 服务基础地址，用于生成短链地址
# 3. DB_URL: 数据库连接地址，支持 SQLite、PostgreSQL 等
# 4. JWT_SECRET_KEY: JWT 签名密钥，生产环境请务必设置一个长且随机的字符串
# 5. ACCESS_TOKEN_EXPIRE_MINUTES: Token 过期时间，默认 1440 分钟 (24 小时)
# 6. ALLOW_PUBLIC_REGISTRATION: 是否允许公开注册，默认 false
# 7. INVITE_CODE: 邀请码（可选），设置后需要邀请码才能注册
# 8. MIN_PASSWORD_LENGTH: 最小密码长度，默认 6

## 首次使用步骤
# 1. 安装依赖: pip install -r requirements.txt
# 2. 创建 .env 文件并配置 (可选)
# 3. 创建管理员账号: python init_admin.py
# 4. 启动服务: uvicorn shortener_app.main:app --reload
# 5. 访问: http://localhost:8000

## 邀请码模式说明
# - 如果设置了 INVITE_CODE，用户注册时必须提供正确的邀请码
# - 如果没有设置 INVITE_CODE 且 ALLOW_PUBLIC_REGISTRATION=false，只有管理员能创建用户
# - 如果 ALLOW_PUBLIC_REGISTRATION=true，则任何人都可以注册（不推荐用于内部服务）

## 管理员功能
# 管理员登录后可以:
# 1. 通过 /docs 或 API 创建新用户 (POST /admin/users)
# 2. 查看所有用户列表 (GET /admin/users)
# 3. 启用/禁用用户 (PUT /admin/users/{user_id}/toggle-active)
# 4. 重置用户密码 (PUT /admin/users/{user_id}/reset-password)
# 5. 管理所有用户的短链
